# Dam Production Environment — copy to /etc/dam/env.prod, chmod 600, root only
# DO NOT commit actual values to git.

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=dam
DAM_DSN=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@dam_postgres:5432/${POSTGRES_DB}

REDIS_PASSWORD=
DAM_REDIS=redis://:${REDIS_PASSWORD}@dam_redis:6379/0

DAM_SECRET_KEY=

DAM_POSTER_ROOT=/dam_posters
DAM_DATA_ROOT=/opt/dam/dam_data

TZ=Asia/Seoul
