# nfs-poc.4 source-analyze — 11.NEXT_UI_2022_10월오픈 분석

> 대상: `\\designfs.ktalpha.com\DESIGNFS\디자인파트\11.NEXT_UI_2022_10월오픈`
> 마운트: `/mnt/designfs/...` (읽기전용)
> 측정일: 2026-06-29 (robocopy `/L` 라이브 열거)

## 1. 라이브 전체 규모 (robocopy /L, 측정 4분)

| 항목 | 값 |
|---|---|
| 디렉토리 | 88,397 |
| 파일 | **280,746** |
| 용량 | 5,477,382,493,803 bytes ≈ **5.48 TB** |

> ⚠ python `os.walk`+`getsize` 는 drvfs/SMB 라운드트립 지연으로 비현실적(276k 파일에 100분+ 추정). **robocopy `/L /MT` 가 SMB 열거에 적합**(stat 병렬화, 4분). 분석/검증은 robocopy 기준으로 수행.

## 2. 확장자 분포 (인벤토리 기준, scan-analysis-2.md, 2026-04-22)

robocopy `/L` 는 확장자별 분해를 제공하지 않으므로, 타입 분포는 직전 UTF-16 인벤토리 스캔값을 사용한다.

| 확장자 | 파일수 | 용량 | 처리 |
|---|---|---|---|
| .psd | 75,166 | 4,636 GB | **제외** |
| .psb | 120 | 183 GB | **제외** |
| .jpg | 119,626 | 43 GB | 복사 |
| .png | 40,359 | 5 GB | 복사 |
| .ai | 142 | 2 GB | 복사 |
| (기타 소형) | ~40k | ~9 GB | 복사 |

- 인벤토리 총계: 4,878 GB / ~276k.
- psd+psb = 75,286 파일 / **4,819 GB (전체 용량의 98.8%)**.

## 3. 제외 규칙 (확정)

- **확장자**: `.psd .psb .zip .7z .rar .tar .gz .alz .mp4 .mov .avi .mkv .wmv .m4v`
- **노이즈**: `Thumbs.db .DS_Store desktop.ini *.lnk ._* ~$*`
- 정책: **제외목록 기반**(나열한 것만 빼고 나머지 복사) — 필요한 디자인 타입 누락 방지.

## 4. ⚠ 핵심 발견 — 복사 타깃 불확실 (4월 대비 +600 GB 증가)

- 인벤토리(4월) 비-psd/psb 용량 = 4,878 − 4,819 = **약 59 GB** → 당초 PoC 복사 타깃 추정치.
- 그러나 **라이브 전체가 5,477 GB** 로 4월(4,878 GB) 대비 **약 600 GB 증가**.
- 이 600 GB 증가분의 구성이 불명확:
  - 증가분이 **이미지(jpg/png)** 면 → 복사 타깃 ≈ **최대 ~658 GB**
  - 증가분이 **psd/zip/영상(제외 대상)** 이면 → 복사 타깃 ≈ **~59 GB 수준 유지**
- **결론**: 복사 타깃은 **59 GB ~ 658 GB 범위**. 정확한 값은 제외필터 적용 robocopy `/L` 실측 필요.

## 5. 실복사 결과 (nfs-poc.5 확정, 2026-06-29)

> robocopy 실복사 완료. 소스→대상: DESIGNFS→DESIGNFS1\dam_dev\11.NEXT_UI_2022_10월오픈

| 항목 | 값 |
|---|---|
| **복사 파일수** | **167,679 파일** |
| **복사 용량** | **60.4 GB** (60,447,685,300 bytes) |
| 제외 파일수 | 113,069 (psd·psb·zip·영상) |
| 제외 용량 | ~5.05 TB |
| 소스 전체 | 280,748 파일 / 5.48 TB |
| 복사 소요 시간 | **1:08:54** |
| robocopy EXIT | 0 (성공) |

> ✅ 복사 타깃 59 GB~658 GB 추정범위 → **60.4 GB** 로 확정(하한 근사값). 4월 인벤토리 비-psd/psb 59 GB 와 거의 일치.
> → 600 GB 증가분은 psd/zip/영상(제외 대상) 위주였음이 확인됨. DESIGNFS1 여유(64 TB) 대비 무리 없음.
> → 썸네일/CLIP 부하도 원래 예상(59 GB / ~160k건) 수준으로 유지.

## 6. 다음 단계 영향

- nfs-poc.5 복사 완료: **167,679 파일 / 60.4 GB** → DESIGNFS1 적재, DESIGNFS1 여유 충분.
- nfs-poc.6 scan-ingest: `DAM_REALM=designfs1_mirror POC_ROOT=/mnt/designfs1/dam_dev/11.NEXT_UI_2022_10월오픈` 으로 DB 적재 (167k건 예상).
- 썸네일/CLIP 부하 = 167k파일 × 평균 ~360 KB ≈ 60 GB — 원래 예상 범위. 로컬 썸네일 압박 없음.
- 원본 불변: DESIGNFS -o ro 마운트 + copy-only(제외 `/MOVE /MIR /PURGE`) → 소스 0 변경 확인.
